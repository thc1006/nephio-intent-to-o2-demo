apiVersion: tna.tna.ai/v1alpha1
kind: IntentDeployment
metadata:
  name: test-sample-2
  namespace: default
spec:
  intent: |
    {
      "intentExpectations": [
        {
          "intentType": "ManagedElementIntentExpectation",
          "targets": [{"name": "edge2"}],
          "context": [{"contextType": "SliceContext", "sliceType": "URLLC"}],
          "intentParameters": {"networkSliceRef": "slice-urllc-002"}
        }
      ]
    }
  deliveryConfig:
    targetSite: "edge2"