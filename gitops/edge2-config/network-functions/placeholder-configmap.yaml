# Placeholder ConfigMap for network-functions
# This will be replaced when actual network function deployments are added via intent pipeline
apiVersion: v1
kind: ConfigMap
metadata:
  name: nf-placeholder
  namespace: oran-nfs
  annotations:
    config.nephio.org/description: "Placeholder for network function deployments"
data:
  info: |
    Network functions will be deployed here when:
    1. An intent is received with targetSite: edge2
    2. The intent is processed and rendered to KRM
    3. The KRM is committed to this repository
    4. Config Sync syncs the changes to the edge2 cluster