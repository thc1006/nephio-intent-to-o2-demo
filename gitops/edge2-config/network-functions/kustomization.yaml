apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: oran-nfs

# Network function deployments will be added here by the intent pipeline
# Currently using a placeholder to maintain valid kustomization structure

resources:
  - placeholder-configmap.yaml

# Common labels for network functions
commonLabels:
  site: edge2
  component: network-function
  managed-by: gitops