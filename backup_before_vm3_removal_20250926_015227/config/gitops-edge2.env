# GitOps Configuration for Edge2 Site
# This file contains environment variables for configuring the RootSync
# Replace placeholder values with your actual repository details

# Git repository URL (HTTPS or SSH format)
# Example: https://github.com/organization/repo.git
# Example: git@github.com:organization/repo.git
GITOPS_REPO_URL=https://github.com/thc1006/nephio-intent-to-o2-demo.git

# Git branch to track
# Example: main, master, develop
GITOPS_BRANCH=main

# Path within the repository containing edge2 manifests
# Example: sites/edge2, deployments/edge2, clusters/edge2
GITOPS_PATH=sites/edge2

# Auth method: none, token, ssh
# Use "none" for public repos, "token" for private with token, "ssh" for SSH key auth
GITOPS_AUTH=none

# Sync period in seconds (how often to check for updates)
# Default: 15s
GITOPS_SYNC_PERIOD=15s

# Optional: Git secret name (if using token or ssh auth)
# GITOPS_SECRET_NAME=git-creds

# Optional: For token auth - GitHub/GitLab personal access token
# GITOPS_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# Optional: For SSH auth - path to SSH private key
# GITOPS_SSH_KEY_PATH=/path/to/ssh/key