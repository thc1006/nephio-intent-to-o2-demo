---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: intentdeployments.tna.tna.ai
spec:
  group: tna.tna.ai
  names:
    kind: IntentDeployment
    listKind: IntentDeploymentList
    plural: intentdeployments
    singular: intentdeployment
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: IntentDeployment is the Schema for the intentdeployments API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of IntentDeployment
            properties:
              compileConfig:
                description: CompileConfig contains settings for the intent compilation
                  process
                properties:
                  engine:
                    default: kpt
                    description: Engine specifies the compilation engine to use
                    enum:
                    - kpt
                    - kustomize
                    - helm
                    type: string
                  renderTimeout:
                    default: 5m
                    description: RenderTimeout is the maximum time allowed for rendering
                    type: string
                type: object
              deliveryConfig:
                description: DeliveryConfig contains settings for resource delivery
                properties:
                  gitOpsRepo:
                    description: GitOpsRepo is the repository URL for GitOps sync
                    type: string
                  syncWaitTimeout:
                    default: 10m
                    description: SyncWaitTimeout is how long to wait for sync completion
                    type: string
                  targetSite:
                    default: both
                    description: TargetSite specifies where to deploy (edge1|edge2|both)
                    enum:
                    - edge1
                    - edge2
                    - both
                    type: string
                type: object
              gatesConfig:
                description: GatesConfig contains SLO/validation gate configurations
                properties:
                  enabled:
                    default: true
                    description: Enabled determines if gates are active
                    type: boolean
                  postCheckScript:
                    description: PostCheckScript is a custom validation script path
                    type: string
                  sloThresholds:
                    additionalProperties:
                      type: string
                    description: SLOThresholds contains minimum acceptable metrics
                    type: object
                type: object
              intent:
                description: Intent represents the original natural language or JSON
                  intent
                type: string
              rollbackConfig:
                description: RollbackConfig contains rollback policies and triggers
                properties:
                  autoRollback:
                    default: true
                    description: AutoRollback enables automatic rollback on failure
                    type: boolean
                  maxRetries:
                    default: 3
                    description: MaxRetries before giving up
                    type: integer
                  retainFailedArtifacts:
                    default: true
                    description: RetainFailedArtifacts keeps debug information
                    type: boolean
                type: object
            required:
            - intent
            type: object
          status:
            description: status defines the observed state of IntentDeployment
            properties:
              compiledManifests:
                description: CompiledManifests contains the generated KRM manifests
                type: string
              conditions:
                description: conditions represent the current state of the IntentDeployment
                  resource
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              deliveryStatus:
                description: DeliveryStatus tracks GitOps sync state
                properties:
                  gitCommit:
                    description: GitCommit is the commit SHA for this deployment
                    type: string
                  sites:
                    additionalProperties:
                      description: SiteStatus represents deployment status for a single
                        site
                      properties:
                        lastSyncTime:
                          description: LastSyncTime when resources were last synced
                          format: date-time
                          type: string
                        message:
                          description: Message provides human-readable status
                          type: string
                        state:
                          description: State of deployment at this site
                          enum:
                          - Pending
                          - Deploying
                          - Deployed
                          - Failed
                          type: string
                      type: object
                    description: Sites contains per-site deployment status
                    type: object
                  syncState:
                    description: SyncState represents the GitOps sync status
                    enum:
                    - Pending
                    - Syncing
                    - Synced
                    - Failed
                    type: string
                type: object
              lastUpdateTime:
                description: LastUpdateTime is the last time the status was updated
                format: date-time
                type: string
              observedGeneration:
                description: ObservedGeneration reflects the generation of the most
                  recently observed IntentDeployment
                format: int64
                type: integer
              phase:
                description: Phase represents the current lifecycle phase
                enum:
                - Pending
                - Compiling
                - Rendering
                - Delivering
                - Validating
                - Succeeded
                - Failed
                - RollingBack
                type: string
              rollbackStatus:
                description: RollbackStatus tracks rollback operations
                properties:
                  active:
                    description: Active indicates if rollback is in progress
                    type: boolean
                  attempts:
                    description: Attempts made so far
                    type: integer
                  previousCommit:
                    description: PreviousCommit being restored
                    type: string
                  reason:
                    description: Reason for the rollback
                    type: string
                type: object
              validationResults:
                description: ValidationResults contains gate check outcomes
                items:
                  description: ValidationResult represents a single validation outcome
                  properties:
                    message:
                      description: Message provides details about the result
                      type: string
                    metrics:
                      additionalProperties:
                        type: string
                      description: Metrics contains numerical results
                      type: object
                    name:
                      description: Name of the validation check
                      type: string
                    passed:
                      description: Passed indicates if the check succeeded
                      type: boolean
                  required:
                  - name
                  - passed
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
